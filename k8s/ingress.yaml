apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-app-ingress
  namespace: demo-app
spec:
  ingressClassName: gce
  defaultBackend:
    service:
      name: demo-app-service
      port:
        number: 80